apiVersion: apps/v1
kind: Deployment
metadata:
  name: token-info-frontend
  # namespace: token-info-page
  labels:
    app: token-info-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: token-info-frontend
  template:
    metadata:
      labels:
        app: token-info-frontend
    spec:
      containers:
      - name: token-info-frontend
        image: gcr.io/fetch-ai-sandbox/token-info-frontend:4369605

        env: 
        - name: PGUSER
          valueFrom:
            secretKeyRef:
              name: token-info-db-credentials
              key: PGUSER

        - name: PGPASSWORD
          valueFrom:
            secretKeyRef:
              name: token-info-db-credentials
              key: PGPASSWORD

        - name: PGHOST
          valueFrom:
            secretKeyRef:
              name: token-info-db-credentials
              key: PGHOST

        - name: PGDATABASE
          valueFrom:
            secretKeyRef:
              name: token-info-db-credentials
              key: PGDATABASE

        - name: ETHERSCAN_API_KEY
          valueFrom:
            secretKeyRef:
              name: contractmon-api-keys
              key: etherscanapikey



        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "1000m"

        ports:
        - containerPort: 8000
